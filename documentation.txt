MeasUnit:
    Hard (not simple like a "m^2", "unit" or "kg") MeasUnits must be
    written according to the syntax:

    1)
    2)


    Examples:
            "work": ["A", "work", [                     // the name of unit, its symbol and category
                    ["mass", 1], ["length", 2],
                    ["time", 2]
                    ]],